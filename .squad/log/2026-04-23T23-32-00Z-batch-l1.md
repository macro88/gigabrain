# Session Log: Batch L1 Implementation & Proof Closure

**Session:** 2026-04-23T23:32:00Z  
**Scope:** Vault-Sync Batch L1 restore-orphan startup recovery implementation and proof lane closure  
**Participants:** Fry (implementation), Scruffy (proof), Professor (pre-gate), Nibbler (adversarial review)  

## Arc

Batch L1 closed as a narrowed slice: restore-orphan startup recovery only, after Batch K2 proved the happy offline restore path. 

### Implementation (Fry)

- Fixed startup order: stale-session sweep → own-session registration → ownership claim → RCRT recovery → supervisor spawn
- Registry-only half of 11.1 (supervisor_handles + dedup bookkeeping); 11.1b (sentinel-directory) deferred to L2
- One shared 15s stale-heartbeat threshold for startup recovery decisions
- Recovery callable only through `finalize_pending_restore(..., FinalizeCaller::StartupRecovery { session_id })`
- Validation: ✅ default lane, ✅ online-model lane

### Proof Lane (Scruffy)

- 11.1a: startup-order evidence with no supervisor-ack residue
- 17.5ll: shared 15s heartbeat gate, exact-once finalize, fresh-heartbeat defer, collection_owners ownership truth
- 17.13: real crash-between-rename-and-Tx-B recovery with state transitions

All 11 mandatory proofs from Nibbler gate addressed and documented. Scope guardrail maintained: proof lane covers restore-owned pending-finalize state only, does NOT support generic needs_full_sync, remap attach, sentinel recovery, or broader "serve heals dirty collections" claims.

### Decisions Merged

1. **Fry decision** — CLI truth for L1 narrowed boundary; MCP deferred
2. **Scruffy decision** — Proof lane confirmation; scope guardrail explicit
3. **Professor pre-gate** — Boundary approved; non-negotiable implementation constraints documented
4. **Nibbler pre-gate** — Narrowed slice adversarially safe; mandatory proofs listed; review caveat on scope-creep

### Gate Status

✅ Pre-implementation gates (Professor + Nibbler) satisfied. L1 approved for landing.

## Team Memory

- L1 is narrowed to restore-orphan startup recovery only
- All deferred work (sentinel-directory, platform gating, broader startup recovery) belongs to L2+
- Startup order is canonical; registry initialization is fatal-fail; stale threshold is shared
- Fresh-heartbeat defer is non-negotiable; ownership truth comes from collection_owners only
- Proof lane is not a general startup-healing endorsement; scope boundaries explicit in all test names/documentation

## Orchestration Notes

- No inbox dedup conflicts; all 4 decisions merged cleanly
- Cross-agent histories updated
- Ready for git commit of `.squad/` changes
