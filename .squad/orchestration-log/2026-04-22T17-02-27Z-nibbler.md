# Nibbler — Vault Sync Batch E Regate (Approved)

**Session:** 2026-04-22T17:02:27Z  
**Status:** approved  
**Scope:** Batch E adversarial review (regate after repair)  

## Prior Rejection

Initial rejection on whole-file-size hash guard in `src/core/reconciler.rs`:
- Guard used whole-file size instead of post-frontmatter body bytes
- Large-frontmatter / tiny-body notes could incorrectly satisfy 64-byte threshold
- Risk: template notes inherit wrong page identity → permanent data loss

## Repair Changes

Leela narrowly repaired the hash-rename guard:
- Missing-side significance now comes from trimmed DB body content (`compiled_truth.trim() + timeline.trim()`)
- New-side significance now comes from trimmed post-frontmatter body returned by `parse_frontmatter()`
- `hash_refusal_reason()` gates on `body_size_bytes < 64`, not whole-file size
- Refusal reason strings updated for clarity

## Regate Validation

✅ **Specific exploit closed:**
- Note can no longer satisfy 64-byte threshold by stuffing bytes into frontmatter
- Refusal path tested directly at helper boundary
- Classification path tested end-to-end: whole-file-large / body-tiny note → `hash_renamed = 0`, quarantine

✅ **Surrounding scope stays honest:**
- `tasks.md` still says native pairing is interface-only (watcher deferred)
- `tasks.md` still says rename/quarantine decisions are classification-only until task 5.5
- `reconciler.rs` still says `migrate::import_dir()` is the live ingest path
- Full walk + hash + apply pipeline not yet implemented

✅ **Positive case preserved:**
- Hash pairing allowed only when body content clears minimum
- Documents safety boundary clearly

## Verdict

**APPROVE** — Prior adversarial blocker is resolved. Batch E is landable.
