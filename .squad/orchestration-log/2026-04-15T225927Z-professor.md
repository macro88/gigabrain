# Orchestration Log: Professor — Phase 2 Early Peer Review

**Timestamp:** 2026-04-15T22:59:27Z  
**Agent:** Professor (Peer Reviewer)  
**Task:** Phase 2 early review gate (Groups 1, 5, 9)  
**Status:** completed  

## Deliverables

1. **4 blocking findings** against current implementation:
   - F1: Graph traversal undirected vs spec outbound-first mismatch
   - F2: Edge deduplication missing on cyclic graphs
   - F3: Progressive retrieval not started; query path still Phase 1 behavior
   - F4: OCC erosion risk in Group 9 MCP writes

2. **Non-blocking maintainability guidance** provided
3. **Sign-off status:** Graph BFS (not approved), Progressive logic (not approved), OCC (not approved yet)
4. **Decision artifacts:** `.squad/decisions/inbox/professor-phase2-review.md`

## Blocking Actions Required

### F1: Graph Contract Mismatch
Choose one contract now:
- If neighbourhood = undirected adjacency: update spec + CLI renderer to show both directions
- If neighbourhood = outbound traversal: remove inbound BFS from core

### F2: Edge Deduplication
Deduplicate edges by stable identity (prefer link ID, else `(from,to,relationship,valid_from,valid_until)`). Add cyclic graph test asserting duplicate-free output.

### F3: Progressive Retrieval
Settle contract before coding. Simplify spec/design now or implement richer surface (linked-page expansion). Currently guaranteed rework.

### F4: OCC Discipline
Preserve Phase 1 OCC on every new page-scoped write tool OR amend spec before implementation. Do not quietly weaken write contract.

## Guidance (Non-blocking)

- Avoid repeated `SELECT slug` inside BFS loop; carry slug in queue or fetch with both endpoints
- Replace broad "task number" comments in tests with helper names or submodules
- Keep `GraphError` typed in core; map to CLI/MCP errors at boundary only

## Review Scope

| Item | Status |
|------|--------|
| Graph BFS | Not approved |
| Progressive budget logic | Not approved |
| OCC unchanged | Not approved yet |

## Coordination

Professor reviews after each group lands (not end-only). Fry should use these blockers as spec-clarification gates before proceeding. Synchronous blocking until F1–F4 are resolved.

## Notes

Review gates are active. Fry cannot merge Groups 1, 5, 9 until Professor sign-off. Current gaps are architectural, not cosmetic.
