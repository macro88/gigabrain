# Scruffy: Assertions/Check Coverage Slice (Tasks 3.1–4.5)

- **Date:** 2026-04-15T23:15:50Z
- **Status:** COMPLETED ✅
- **Scope:** `src/core/assertions.rs`, `src/commands/check.rs`, `tests/assertions.rs`, `openspec/changes/p2-intelligence-layer/tasks.md`

## Key decisions

### D1: Preserve manual assertions on re-index

When `extract_assertions()` re-indexes a page, only prior `asserted_by = 'agent'` rows are replaced. Manual assertions (`asserted_by = 'manual'`) survive re-indexing, preserving human judgment across automated contradiction detection reruns.

### D2: Pure helper seam for testability

`commands::check` delegates to pure `execute_check()` plus render helpers. This keeps task 4.5 coverage branch-focused: tests validate page targeting, `--all` processing, JSON shape, and existing contradiction reporting without binding to terminal plumbing or stdout-capture tricks.

## Rationale

Nibbler's Phase 2 guardrails explicitly require contradiction reruns to stay idempotent and manual assertions to survive re-indexing. The pure helper seam enables deterministic coverage validation.

## Validation

- `cargo test --lib assertions`: all tests pass.
- `cargo test --test check`: all integration tests pass.
- `cargo clippy -- -D warnings`: clean.
- `cargo fmt --check`: clean.
- Decision inbox file: `.squad/decisions/inbox/scruffy-assertions-coverage.md`

## Landing decision

**APPROVE FOR LANDING** (assertions/check tasks 3.1–4.5). Architecture matches Phase 2 guardrail requirements. Coverage seam is production-ready.
