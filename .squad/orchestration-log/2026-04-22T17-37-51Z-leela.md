# Orchestration — Leela: Vault-Sync Foundation Repair

**Timestamp:** 2026-04-22T17:37:51Z  
**Agent:** Leela  
**Topic:** vault-sync-engine foundation slice repair  
**Requested by:** macro88  

---

## Scope

Vault-sync foundation slice (`openspec/changes/vault-sync-engine`, tasks 1.1–2.6 subset) was rejected by Professor for schema coherence issues and 181 test failures. Leela executed integration-focused repair pass.

---

## Work Performed

### 1. Schema Compatibility Repair (`src/schema.sql`, `src/core/db.rs`)

- **Decision 1:** Added `DEFAULT 1` to `pages.collection_id` with auto-creation of default collection via `ensure_default_collection()` at every `open_connection()`. All legacy INSERT statements now have a valid FK target without touching 20+ insert sites.
- **Decision 2:** Changed `pages.uuid` from `NOT NULL` to nullable (`DEFAULT NULL`) with partial unique index (`WHERE uuid IS NOT NULL`), deferring UUID lifecycle (tasks 5a.1–5a.7) to later implementation phase.
- **Decision 3:** Retained `ingest_log` table as compatibility shim until watcher/reconciler slice explicitly removes it.
- **Decision 4:** Updated all upsert paths in `ingest.rs` and `migrate.rs` to use `ON CONFLICT(collection_id, slug)` matching the new unique constraint.
- **Decision 5:** Added `AND p.quarantined_at IS NULL` filter to `search_vec` in `inference.rs` to align vector search quarantine handling with FTS5.

### 2. Test Results

- **Before repair:** `cargo test` reported **181 failing tests** across `commands::check`, `core::fts`, `core::inference`, and other test bins.
- **After repair:** `cargo test` reports **0 failures** across all test bins.

### 3. Task Truthfulness Update

- Updated `tasks.md` to reflect actual completion status:
  - Task 1.1: marked complete only after schema tables exist and tests pass
  - Task 1.6: marked complete only after quarantine filtering is wired through all search paths
  - Task 2.6: marked complete only after stable user-facing error shape is serialized

### 4. Legacy Schema Gating

- Fixed `open_connection()` schema version detection: v5 schema execution now happens only after legacy version check succeeds or `brain_config` confirms v5.
- Pre-`brain_config` databases now properly rejected with re-init instructions instead of falling into legacy path.

---

## Outcome

- **Verdict:** Foundation slice is now coherent and test-clean. Schema, collections, and quarantine filtering are wired. Legacy write paths and existing test surfaces are compatible with v5 schema.
- **Test Status:** All 181 prior failures resolved. Full `cargo test --quiet` passes.
- **Unblocked:** Follow-on implementation batches can now safely build on vault-sync foundation.

---

## Decision Artifact

Merged to canonical `decisions.md` (1-5 above).

---

## Scoped Out (For Later Phases)

- `serve_sessions`, `collection_owners` schema completion (watcher slice)
- UUID generation lifecycle (tasks 5a.1–5a.7)
- `brain_gap` slug binding (tasks 1.1b–1.1c)
- Watcher, reconciler, fs-safety, and collection-ownership tasks (sections 3–6, 5a)

