# Orchestration Log — Leela

**Date:** 2026-04-22T08:47:14Z  
**Agent:** Leela  
**Mode:** background  
**Status:** completed  

## Summary

Decomposed the complete `vault-sync-engine` OpenSpec change (370+ tasks, 18 groups, v4→v5 breaking schema change) into **9 implementation waves** with **3 gated PRs** (Foundation, Live Engine, Full Surface).

## Work Completed

1. **Architecture decomposition:** Mapped 370+ tasks into 9 sequential waves with dependency graph, identifying critical path (Schema → Collections → Reconciler → Watcher+brain_put → Commands/Serve → MCP).

2. **Risk identification:** Located highest-risk items:
   - Wave 3 (Reconciler): two-phase restore/remap defense — 8 sub-tasks, multi-phase restore/remap with lease-based coordination, stability checks, fence diffs, four named error types.
   - Wave 5 (brain_put): 13-step rename-before-commit sequence with crash-safety, recovery sentinel lifecycle, IPC socket write proxying.
   - Wave 4: Watcher overflow real-time constraint (needs_full_sync → full_hash_reconcile within ~1s).
   - Wave 6: RCRT (Restoring-Collection Retry Task) and online restore handshake.

3. **Implementation slicing:** Proposed three gated PRs:
   - **PR A — Foundation:** Waves 1–2 (schema v5, collections CRUD, fs_safety, UUID lifecycle, ignore patterns, foundation tests). Exit gate: `cargo test` passes; v5 schema; collection CRUD works.
   - **PR B — Live Engine:** Waves 3–5 (reconciler, watcher, brain_put write-through, engine tests). Exit gate: crash-safety tests pass; watcher 2s latency test passes.
   - **PR C — Full Surface:** Waves 6–7, 9 (commands, serve, MCP awareness, legacy removal, docs). Exit gate: `gbrain collection add <vault>` → MCP query returns fresh content within 2s; 90%+ coverage; import.rs removed.

4. **First execution batch:** Locked PR A foundation batch (tasks 1.1–1.6, 2.1–2.6, 2.4a–2.4d, 3.1–3.7, 4.1–4.4, 5a.1–5a.4a, 17.1–17.4). Scope: ~1 week, Fry implementation.

5. **Open questions resolved:** Documented 10 open questions with recommendations:
   - Branch strategy: cut fresh feature branches from contributor's branch (spec source).
   - Active in-flight work: resolve v0.9.3/v0.9.4 BEFORE starting vault-sync-engine.
   - Windows CI: add `cargo check --target x86_64-pc-windows-gnu` in PR A.
   - IPC security: Nibbler pre-implementation adversarial review (12.6c–g).
   - raw_imports audit: explicit callsite audit before Wave 3.
   - macOS CI: add macOS runner for vault-sync test suite.
   - Cargo.toml deps: dry-run `cargo add` for conflicts (notify, ignore, globset, rustix, uuid v7).
   - Import removal lint: CI verifies no `.md` references to `gbrain import` unless `import.rs` exists.
   - Coverage hard gate: add `cargo llvm-cov --fail-under-lines 90` in PR A.
   - Users with existing v4 DBs: re-init error must mention `gbrain export` escape hatch.

6. **Artifact produced:** `.squad/decisions/inbox/leela-vault-sync-breakdown.md` (305 lines, comprehensive execution roadmap).

## Decision

Keep vault-sync-engine as ONE OpenSpec change, implement in three gated PRs. Start with PR A foundation batch. Nibbler must review IPC security before Wave 5 begins. Bender + Scruffy track 90%+ coverage with every PR. Resolve 10 open questions before/during Wave 1.

## Recommendations to Team

1. Land v0.9.3/v0.9.4 work first to avoid schema merge conflicts.
2. Cut fresh feature branches from contributor's branch (read-only spec source).
3. Treat `cargo llvm-cov --fail-under-lines 90` as a hard CI gate added in PR A.
4. Nibbler adversarial review of IPC socket protocol (12.6c–g) must complete before Wave 5 implementation begins.
5. Windows CI gate should verify platform gate compiles, not require Windows compatibility.
