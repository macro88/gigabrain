# Orchestration Log: Scruffy

**Date:** 2026-04-15T23:31:37Z  
**Agent:** Scruffy  
**Status:** Completed  
**Scope:** Graph cycle/self-loop render suppression  

## Summary

Completed graph render cycle/self-loop suppression (commit `acd03ac`). Self-loop edges and cycles that return to the root no longer print the root as its own neighbour in human output. Edge traversal remains safe (cycle detection via visited set); human-facing output contract now matches specification.

## Decision

Merged into canonical `decisions.md`: self-loop and cycle-back edges suppressed from human `gbrain graph` text rendering before print (not only before recursion).

## Fixes Applied

- Self-edge on root no longer appears as `→ <root> (self)`
- Cycles `a → b → a` no longer print root back into the tree
- Traversal termination unchanged (safe via visited set)
- Regression tests cover both edge cases

## Validation

- `cargo test graph --quiet` ✅
- `cargo test --quiet` ✅
- `cargo clippy --quiet -- -D warnings` ✅
- `cargo fmt --check` ✅
