# Orchestration: Scruffy — Vault Sync Batch G Coverage

**Date:** 2026-04-22  
**Agent:** Scruffy  
**Session:** vault-sync-batch-g-approval  
**Role:** Tester / coverage author  
**Status:** ✅ Completed

## Work output

Designed Batch G coverage strategy: keep active coverage on implemented seams only; lock deferred surfaces with explicit seam blockers instead of silent omission or pretending completion.

### Decisions

1. **Active coverage seams** (tested and passing)
   - reconcile unchanged path: one active raw_import, no rotation
   - reconcile changed-hash apply path: rotates raw_imports to latest bytes
   - reconcile aborts before mutation on zero-active existing raw_imports
   - brain_put preserves stored pages.uuid when input omits gbrain_id

2. **Deferred coverage seams** (locked with visible blockers)
   - `full_hash_reconcile` unchanged-hash self-heal → blocker: 4.4 (test name: `4a.4`, status: ignored)
   - `full_hash_reconcile` changed-hash rotation → blocker: 4.4 (test name: `5.4h`, status: ignored)
   - render_page UUID back-fill for pages lacking frontmatter.gbrain_id → blocker: 5a.5 (test name: `5a.6`, status: ignored)

### Rationale

Truth over silence: the current tree supports direct branch validation for the reconcile/put slice. `full_hash_reconcile` and UUID re-emission from legacy pages are still unimplemented and need visible seam locks in the test suite, not silent omission. Ignored test markers with explicit task IDs prevent silent test-suite shrinkage.

### Coverage landmarks

- ✅ Unchanged hash: single active row verified, no rotation mutation
- ✅ Changed hash: atomic raw_imports rotation (old deactivated, new inserted)
- ✅ Zero-active abort: InvariantViolationError before any page/file_state/raw_imports mutation
- ✅ brain_put round-trip: pages.uuid preserved when gbrain_id omitted from input
- ✅ Render null uuid: frontmatter does not emit gbrain_id field

### Test structure

```
tests/vault_sync_batch_g.rs
├── reconcile_unchanged_path_keeps_active_row_no_rotation
├── reconcile_changed_path_rotates_raw_imports_atomically
├── reconcile_fails_before_mutation_on_zero_active
├── put_preserves_stored_uuid_when_gbrain_id_omitted
├── render_null_uuid_does_not_emit_gbrain_id [seam lock: 5a.6]
├── #[ignore = "blocker: 4.4"] full_hash_reconcile_unchanged_self_heals
├── #[ignore = "blocker: 4.4"] full_hash_reconcile_changed_rotates
├── #[ignore = "blocker: 5a.5"] render_backfills_gbrain_id_for_legacy_pages
```

### Gate status

- [x] Active seams validated by direct test
- [x] Coverage lane truthful (no pretense; no silent omission)
- [x] Seam locks visible with task blockers
- [x] No red suite; no broken trunk
- [x] cargo test passes

---

*Scruffy coverage strategy locked*
