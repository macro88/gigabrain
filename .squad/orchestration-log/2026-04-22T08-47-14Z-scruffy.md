# Orchestration Log — Scruffy

**Date:** 2026-04-22T08:47:14Z  
**Agent:** Scruffy  
**Mode:** background  
**Status:** completed  

## Summary

Assessed current CI/coverage surface against `openspec/changes/vault-sync-engine` requirements; flagged ambiguity in >90% coverage denominator; recommended practical coverage bar and explicit scope decision for repo-wide gate.

## Work Completed

1. **Coverage baseline audit:** Reviewed current repo CI surface in `.github/workflows/ci.yml` and local test infrastructure.
   - Current `cargo llvm-cov report` shows `src/**` at **88.71% line coverage**.
   - CI has informational coverage job that uploads to Codecov but does **not enforce numeric threshold** (`fail_ci_if_error: false`).
   - Highest legacy coverage sinks: `src/main.rs`, `src/commands/call.rs`, `src/commands/timeline.rs`, `src/commands/query.rs`, `src/commands/skills.rs`.

2. **Vault-sync coverage analysis:** Reviewed OpenSpec `vault-sync-engine` surface (Waves 1–9, task groups 1–18).
   - New stateful surfaces (watchers, reconciliation, restore/finalize, write-through recovery, collection routing) can achieve 90%+ line coverage on their seams.
   - BUT: repo-wide >90% promise without legacy backfill would force unrelated cleanup unless denominator is carefully chosen.

3. **Coverage denominator ambiguity:** Flagged three undefined dimensions:
   - **Denominator:** `src` only vs all Rust including tests?
   - **Feature scope:** default only vs default + online-model channels?
   - **OS scope:** Ubuntu-only coverage vs unsupported Windows paths (fd-relative syscalls gate to `#[cfg(unix)]`)?

4. **Practical coverage recommendation:** Proposed two-tier approach:
   - **Tier 1 (per-PR for new/touched vault-sync surfaces):** ≥90% line coverage at the seam (unit + deterministic integration tests).
   - **Tier 2 (repo-wide reporting):** Continue informational coverage reporting, but DO NOT promise hard repo-wide gate unless team explicitly accepts legacy backfill or denominator is scoped to avoid low-coverage CLI files.

5. **Decision gating:** User requires >90% overall. This requires explicit scope decision covering:
   - Is backfill of legacy CLI files (main.rs, commands/*.rs orchestration) part of vault-sync or its own change?
   - Which files are denominator: src only? src + tests?
   - Which feature channels are covered: default only? default + online-model?
   - Which OS scope: Linux-only (current CI) or must Windows/macOS compile gates also be covered?

6. **Artifact produced:** `.squad/decisions/inbox/scruffy-vault-sync-coverage.md` (24 lines, flags ambiguity with practical recommendation).

## Decision

Treat user's >90% overall requirement as **ambiguous until denominator is explicitly defined**. Recommend:
1. Add `cargo llvm-cov --fail-under-lines 90` hard gate as part of PR A (vault-sync foundation).
2. Define scope: which files, which features, which platforms?
3. If macro88 wants repo-wide gate, this requires separate explicit scope decision (likely 0.5–1 day backfill work).

## Recommendations to Team

1. Do NOT promise hard repo-wide 90% gate without defining denominator first.
2. Tier 1: every new vault-sync surface ships with ≥90% coverage at its seam.
3. Tier 2: repo-wide coverage continues to be reported, but remains informational until scope is locked.
4. Add CI gate `cargo llvm-cov --fail-under-lines 90` (configurable denominator) in PR A.
5. If macro88 explicitly wants legacy backfill, scope it separately (likely decreases vault-sync PR review noise).
