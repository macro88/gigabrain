# Orchestration: Fry — Vault Sync Batch G Implementation

**Date:** 2026-04-22  
**Agent:** Fry  
**Session:** vault-sync-batch-g-approval  
**Role:** Implementation author  
**Status:** ✅ Completed

## Work output

Implemented Batch G scope: `full_hash_reconcile`, InvariantViolationError wiring, render_page UUID emission, and targeted UUID tests.

### Tasks completed

1. **4.4 — full_hash_reconcile(collection_id)**
   - Reused `walk_root` + apply pipeline without stat-optimization
   - Accepted closed-mode authorization (FullHashReconcileMode enum)
   - Transaction chunking: 500-file atomic groups (same as reconcile)
   - Updated `last_full_hash_at` per file after rebuild
   - All prerequisites (walk, rename resolution, apply pipeline) confirmed ready

2. **5.4h — InvariantViolationError guard wired**
   - Guarded full_hash_reconcile with zero-active/zero-total preflight
   - `--allow-rerender` audit-logged WARN override (caller responsibility)
   - Used same InvariantViolationError variant as fail-closed pattern

3. **5a.6 — render_page() emits gbrain_id**
   - Single-function change in core/markdown.rs
   - Emit only when pages.uuid is non-empty
   - Preserves existing behavior for legacy rows with NULL/empty uuid

4. **5a.7 (partial) — UUID identity tests**
   - gbrain_id adoption: ingest + assert round-trip
   - brain_put preservation: get → put → assert survives
   - UUIDv7 monotonicity: N generations strictly increasing
   - Frontmatter round-trip: parse/render preserves gbrain_id

### Design decisions

- **Authorization contract:** Closed-mode enum (FullHashReconcileMode) + caller-responsibility documentation (not bare helper)
- **Unchanged-hash handling:** Metadata self-heal only (file_state + last_full_hash_at); no raw_imports rotation
- **UUID write-back seam:** render_page overlay of persisted pages.uuid as gbrain_id (passive path, no active write-through)
- **New-page bootstrap:** Remains narrow; repair does not affect truly new pages (current_page = None → INSERT)

### Test landmarks

- ✅ Unchanged hash: exactly one active raw_imports, no rotation
- ✅ Changed hash: rotation atomic (old deactivated, new inserted)
- ✅ Zero-active rows: InvariantViolationError fires
- ✅ Idempotent: full_hash_reconcile called twice yields ReconcileStats { unchanged: N, modified: 0, ... }
- ✅ NULL uuid: render_page does not emit gbrain_id field
- ✅ MCP round-trip: brain_get + brain_put preserves gbrain_id

### Gate status

- [x] full_hash_reconcile runs to completion
- [x] Idempotent on second call
- [x] Zero-active triggers InvariantViolationError
- [x] --allow-rerender logs WARN correctly
- [x] render_page UUID emission correct for both empty and non-empty uuid
- [x] MCP round-trip preserves gbrain_id
- [x] cargo test and cargo clippy clean

### Deferrals

- 5.8* (restore/remap): Now unblocked by 4.4 → Batch H
- 5.9 (fresh-attach): Needs Group 9 collection commands scaffold → defer
- 4.6 (periodic audit): Needs serve → defer
- 5a.5/5a.5a (UUID write-back / migrate-uuids): Needs Group 12 rename-before-commit → defer

---

*Fry implementation completed*
