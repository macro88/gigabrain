# Scruffy — Vault Sync Batch J Test & Proof Lane

**Date:** 2026-04-23T08:18:25Z  
**Change:** `vault-sync-engine`  
**Batch:** J  

## Outcome

✅ **PROOF LANE COMPLETE AND VALIDATED**

Strengthened proof/tests for narrowed Batch J. Confirmed credible support for all seven narrowed IDs plus two mandatory proofs. All validation gates passed in default and online-model lanes.

## Proof Coverage

### Supported IDs (Implementation Truth)

| ID | What was proven |
|----|-----------------|
| `9.5` | Plain `gbrain collection sync <name>` stays fail-closed on blocked states; active-root reconcile path only |
| `17.5hh` | `collection_owners` PK invariant holds; multi-owner attach remains impossible |
| `17.5hh2` | CLI owner lease released on exit (explicit RAII guard, panic-safe) |
| `17.5hh3` | Short-lived lease acquired, heartbeat renewed during long reconcile, released on all paths |
| `17.5nn` | Duplicate UUID reconcile halt stops plain sync; non-success exit |
| `17.5oo` | Unresolvable trivial content halt stops plain sync; non-success exit |
| `17.5oo3` | CLI `collection info --json` surfaces `integrity_blocked`, `blocked_state`, `suggested_command` |

### Mandatory Proofs (Test Coverage)

**Proof 1: Lease Invariant**
```rust
✅ plain sync acquires exactly one owner lease
✅ heartbeat fires during reconcile work
✅ release succeeds on clean exit
✅ release succeeds on error exit
✅ release succeeds on panic (unwound via test harness)
✅ stale lease cannot be inherited by successor actor
```

**Proof 2: Fail-Closed Gates**
```rust
✅ sync vs state='restoring' → failure with "restore pending" guidance
✅ sync vs needs_full_sync=1 + restoring → failure with guidance
✅ sync vs integrity_failed_at IS NOT NULL → failure with "restore-reset" guidance
✅ sync vs pending_manifest_incomplete_at (escalated) → failure with guidance
✅ sync vs reconcile_halted_at IS NOT NULL → failure with "reconcile-reset" guidance
✅ sync vs ordinary dirty active → succeeds and reports "reconciled active root"
✅ sync does NOT finalize pending restore implicitly
✅ sync does NOT clear needs_full_sync via direct state-flip
```

### CLI Truth Surface

**Collection info (`gbrain collection info --json`)**
```rust
✅ blocked_state distinguishes: null, "restore-pending", "restore-integrity-blocked", "reconcile-halted", "manifest-incomplete"
✅ integrity_blocked set correctly for each blocked state
✅ suggested_command points to correct next action
✅ no success-shaped messaging before reconcile completes
```

## Test Files

**Primary:** `tests/collection_cli_truth.rs`
- 15 test cases covering all narrow-batch scenarios
- Covers all five blocked states + success path
- Covers lease acquire/heartbeat/release lifecycle
- Covers duplicate UUID and trivial content halt triggers

## Validation Results

✅ `cargo test --quiet` (default, BGE-small-en-v1.5)
```
running 15 tests
test collection_cli_truth ... ok
test collection_cli_truth_integrity_blocked ... ok
test collection_cli_truth_reconcile_halted ... ok
test collection_cli_truth_manifest_incomplete ... ok
test collection_cli_truth_restore_pending ... ok
test collection_cli_truth_lease_lifecycle ... ok
... [all pass]
```

✅ `GBRAIN_FORCE_HASH_SHIM=1 cargo test --quiet --no-default-features --features bundled,online-model`
```
running 15 tests
... [all pass with online-model enabled]
```

## Decisions Applied

**Scruffy proof lane decision:** Narrowed batch is supported in code for all seven IDs. CLI truthfulness scoped to `gbrain collection info` rather than new MCP surface. ✅ Applied  

**Fry CLI boundary decision:** MCP surface explicitly deferred. ✅ Confirmed in test scope  

## Narrowed Batch J Closure

The narrowed batch now stands complete with:
- ✅ One real new operator surface (`9.5`) wired only for active-root reconcile
- ✅ Two mandatory proofs (`17.5hh`, `17.5hh2`) proven in code via lease RAII guard
- ✅ Three proof tasks (`17.5hh3`, `17.5nn`, `17.5oo`) implemented and tested
- ✅ One diagnostics surface (`17.5oo3`) made truthful on CLI with JSON output
- ✅ All five blocked-state fail-closed gates tested
- ✅ Reconcile-halt tests verify terminal behavior (no self-healing)
- ✅ Lease lifecycle tests verify singular ownership under concurrent conditions

## Non-Negotiables Verified

✅ Bare sync is active-root reconcile only (no implicit finalize/remap/state-flip)  
✅ Fail-closed on all five blocked states  
✅ needs_full_sync cleared only by real active-root reconcile  
✅ CLI lease is singular, short-lived, released on all exit paths  
✅ Duplicate/trivial halts are terminal, not self-healing  
✅ Operator surfaces truthful; no over-claiming of recovery  
✅ No new IPC/proxy/serve-handshake behavior  

## Ready for

- Final Nibbler adversarial review
- Professor implementation gate confirmation  
- Landing and merge to `spec/vault-sync-engine`

## Deferred Proof Items (Next Batch)

The original larger proof set remains explicitly deferred:
- Ownership/lease/handshake proofs: `17.5hh4`, `17.5pp`, `17.5qq*`
- Restore/remap verification: `17.5ii*`, `17.5ii4`, `17.5ii5`
- Finalize/orphan/manifest: `17.5kk3`, `17.5ll*`, `17.5mm`
- End-to-end: `17.9`, `17.10`, `17.11`, `17.13`

These remain the next destructive-path closure batch.
